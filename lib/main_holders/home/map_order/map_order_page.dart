import 'package:flutter/material.dart';

class MapOrderPage extends StatelessWidget {
  const MapOrderPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text("지도로 주문 페이지"),
    );
  }
}
