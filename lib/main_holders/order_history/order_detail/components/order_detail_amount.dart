import 'package:flutter/material.dart';

class OrderDetailAmount extends StatelessWidget {
  const OrderDetailAmount({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
